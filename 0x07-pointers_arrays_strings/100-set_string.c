#include "main.h"

/**
* set_string - sets value of pointer to char
* @s: pointer that points to pointer to char
* @to: pointer to char
* Return: void
*/

void set_string(char **s, char *to)
{
*s = to;
}
